'use strict';
const hostConfig = {
    domain: '127.0.0.1',
    // 服务器启动端口
    port: 'xxxx'
}

const redisConfig = {
    'host': '127.0.0.1',
    // redis端口
    'port': 'xxxx'
}
const analyticsConfig = {
	// 输入您的ga
    viewId: 'ga:xxxxxxx'
}
export { hostConfig, redisConfig,analyticsConfig };